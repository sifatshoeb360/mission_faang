#include<bits/stdc++.h>
using namespace std;
int main()
{
    int number = 4;
    //string s= (number > 0) ? "Positive Number!" : "Negative Number!";

    cout<<(number > 0) ? "Positive Number!" : "Negative Number!";
}